export type navigationType = {
  image: string;
  name: string;
  href: string;
};

export type integrationType = {
  protocolImage: string;
  title: string;
  description: string;
  networks: string[];
  status: boolean;
};
